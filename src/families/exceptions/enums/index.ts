const ExceptionMessage = {
  CARDANO_REWARD_KEY_IS_REQUIRED: "Reward private key is required for Cardano base address",
  NETWORK_IS_NOT_SUPPORTED: "Network is not supported",
  INVALID_DERIVATION_TYPE: "Invalid derivation type",
} as const;

export { ExceptionMessage };
