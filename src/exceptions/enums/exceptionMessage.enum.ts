const ExceptionMessage = {
  INVALID_DERIVATION_PATH: "Invalid derivation path",
  INVALID_PURPOSE: "Invalid derivation path purpose",
  INVALID_COIN: "Invalid derivation path coin",
} as const;

export { ExceptionMessage };
